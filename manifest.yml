applications:
  - name: Perritos-backend
    memory: 512M
    instances: 1
    buildpacks:
      - python_buildpack
    command: python -m uvicorn main:app --host 0.0.0.0 --port ${PORT}
    env:
      PYTHONUNBUFFERED: "true"